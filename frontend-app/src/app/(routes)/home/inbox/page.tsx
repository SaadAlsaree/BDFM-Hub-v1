import PageContainer from '@/components/layout/page-container';

export const metadata = {
  title: 'قائمة الوارد', // title of the page in browser tab. (optional)
  description: 'inbox description' // meta tag content for "description" property. (optional)
};

const InboxPage = () => {
  return <PageContainer>InboxPage</PageContainer>;
};

export default InboxPage;
